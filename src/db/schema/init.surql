DEFINE TABLE IF NOT EXISTS users SCHEMAFULL;
DEFINE FIELD IF NOT EXISTS chatId ON TABLE users TYPE string;
DEFINE FIELD IF NOT EXISTS phoneNumber ON TABLE users TYPE option<string>;
DEFINE FIELD IF NOT EXISTS referredBy ON TABLE users TYPE option<string>;
DEFINE FIELD IF NOT EXISTS createdAt ON TABLE users TYPE string;
DEFINE FIELD IF NOT EXISTS updatedAt ON TABLE users TYPE string;
DEFINE INDEX IF NOT EXISTS users_chat_id ON TABLE users COLUMNS chatId UNIQUE;

DEFINE TABLE IF NOT EXISTS referral_codes SCHEMAFULL;
DEFINE FIELD IF NOT EXISTS userId ON TABLE referral_codes TYPE string;
DEFINE FIELD IF NOT EXISTS code ON TABLE referral_codes TYPE string;
DEFINE FIELD IF NOT EXISTS createdAt ON TABLE referral_codes TYPE string;
DEFINE INDEX IF NOT EXISTS referral_codes_code ON TABLE referral_codes COLUMNS code UNIQUE;
DEFINE INDEX IF NOT EXISTS referral_codes_user_id ON TABLE referral_codes COLUMNS userId UNIQUE;

DEFINE TABLE IF NOT EXISTS referrals SCHEMAFULL;
DEFINE FIELD IF NOT EXISTS referrerId ON TABLE referrals TYPE string;
DEFINE FIELD IF NOT EXISTS referredUserId ON TABLE referrals TYPE string;
DEFINE FIELD IF NOT EXISTS referralCode ON TABLE referrals TYPE string;
DEFINE FIELD IF NOT EXISTS qualified ON TABLE referrals TYPE bool;
DEFINE FIELD IF NOT EXISTS createdAt ON TABLE referrals TYPE string;
DEFINE INDEX IF NOT EXISTS referrals_referred_user ON TABLE referrals COLUMNS referredUserId UNIQUE;
DEFINE INDEX IF NOT EXISTS referrals_referrer_created ON TABLE referrals COLUMNS referrerId, createdAt;

DEFINE TABLE IF NOT EXISTS wa_sessions SCHEMAFULL;
DEFINE FIELD IF NOT EXISTS sessionId ON TABLE wa_sessions TYPE string;
DEFINE FIELD IF NOT EXISTS creds ON TABLE wa_sessions TYPE option<object> FLEXIBLE;
DEFINE FIELD IF NOT EXISTS keys ON TABLE wa_sessions TYPE option<object> FLEXIBLE;
DEFINE FIELD IF NOT EXISTS status ON TABLE wa_sessions TYPE option<object> FLEXIBLE;
DEFINE FIELD IF NOT EXISTS updatedAt ON TABLE wa_sessions TYPE string;
DEFINE INDEX IF NOT EXISTS wa_sessions_session_id ON TABLE wa_sessions COLUMNS sessionId UNIQUE;

DEFINE TABLE IF NOT EXISTS messages SCHEMAFULL;
DEFINE FIELD IF NOT EXISTS userId ON TABLE messages TYPE string;
DEFINE FIELD IF NOT EXISTS chatId ON TABLE messages TYPE string;
DEFINE FIELD IF NOT EXISTS direction ON TABLE messages TYPE string;
DEFINE FIELD IF NOT EXISTS text ON TABLE messages TYPE string;
DEFINE FIELD IF NOT EXISTS attachmentIds ON TABLE messages TYPE array<string>;
DEFINE FIELD IF NOT EXISTS createdAt ON TABLE messages TYPE string;
DEFINE INDEX IF NOT EXISTS messages_chat_created ON TABLE messages COLUMNS chatId, createdAt;

DEFINE TABLE IF NOT EXISTS attachments SCHEMAFULL;
DEFINE FIELD IF NOT EXISTS userId ON TABLE attachments TYPE string;
DEFINE FIELD IF NOT EXISTS chatId ON TABLE attachments TYPE string;
DEFINE FIELD IF NOT EXISTS filename ON TABLE attachments TYPE option<string>;
DEFINE FIELD IF NOT EXISTS mimeType ON TABLE attachments TYPE string;
DEFINE FIELD IF NOT EXISTS kind ON TABLE attachments TYPE string;
DEFINE FIELD IF NOT EXISTS storagePath ON TABLE attachments TYPE string;
DEFINE FIELD IF NOT EXISTS sha256 ON TABLE attachments TYPE string;
DEFINE FIELD IF NOT EXISTS textContent ON TABLE attachments TYPE option<string>;
DEFINE FIELD IF NOT EXISTS ocrStatus ON TABLE attachments TYPE string;
DEFINE FIELD IF NOT EXISTS createdAt ON TABLE attachments TYPE string;
DEFINE INDEX IF NOT EXISTS attachments_sha256 ON TABLE attachments COLUMNS sha256 UNIQUE;

DEFINE TABLE IF NOT EXISTS resume_documents SCHEMAFULL;
DEFINE FIELD IF NOT EXISTS userId ON TABLE resume_documents TYPE string;
DEFINE FIELD IF NOT EXISTS attachmentIds ON TABLE resume_documents TYPE array<string>;
DEFINE FIELD IF NOT EXISTS source ON TABLE resume_documents TYPE string;
DEFINE FIELD IF NOT EXISTS rawText ON TABLE resume_documents TYPE string;
DEFINE FIELD IF NOT EXISTS extractedAt ON TABLE resume_documents TYPE string;
DEFINE FIELD IF NOT EXISTS updatedAt ON TABLE resume_documents TYPE string;

DEFINE TABLE IF NOT EXISTS resume_profiles SCHEMAFULL;
DEFINE FIELD IF NOT EXISTS userId ON TABLE resume_profiles TYPE string;
DEFINE FIELD IF NOT EXISTS fullName ON TABLE resume_profiles TYPE option<string>;
DEFINE FIELD IF NOT EXISTS email ON TABLE resume_profiles TYPE option<string>;
DEFINE FIELD IF NOT EXISTS phone ON TABLE resume_profiles TYPE option<string>;
DEFINE FIELD IF NOT EXISTS location ON TABLE resume_profiles TYPE option<string>;
DEFINE FIELD IF NOT EXISTS summary ON TABLE resume_profiles TYPE option<string>;
DEFINE FIELD IF NOT EXISTS titles ON TABLE resume_profiles TYPE array<string>;
DEFINE FIELD IF NOT EXISTS skills ON TABLE resume_profiles TYPE array<string>;
DEFINE FIELD IF NOT EXISTS preferredLocations ON TABLE resume_profiles TYPE array<string>;
DEFINE FIELD IF NOT EXISTS industries ON TABLE resume_profiles TYPE array<string>;
DEFINE FIELD IF NOT EXISTS yearsOfExperience ON TABLE resume_profiles TYPE option<number>;
DEFINE FIELD IF NOT EXISTS seniority ON TABLE resume_profiles TYPE option<string>;
DEFINE FIELD IF NOT EXISTS rawText ON TABLE resume_profiles TYPE string;
DEFINE FIELD IF NOT EXISTS sourceAttachmentIds ON TABLE resume_profiles TYPE array<string>;
DEFINE FIELD IF NOT EXISTS updatedAt ON TABLE resume_profiles TYPE string;
DEFINE INDEX IF NOT EXISTS resume_profiles_user_id ON TABLE resume_profiles COLUMNS userId UNIQUE;

DEFINE TABLE IF NOT EXISTS search_profiles SCHEMAFULL;
DEFINE FIELD IF NOT EXISTS userId ON TABLE search_profiles TYPE string;
DEFINE FIELD IF NOT EXISTS summary ON TABLE search_profiles TYPE string;
DEFINE FIELD IF NOT EXISTS targetTitles ON TABLE search_profiles TYPE array<string>;
DEFINE FIELD IF NOT EXISTS relatedTitles ON TABLE search_profiles TYPE array<string>;
DEFINE FIELD IF NOT EXISTS skills ON TABLE search_profiles TYPE array<string>;
DEFINE FIELD IF NOT EXISTS preferredLocations ON TABLE search_profiles TYPE array<string>;
DEFINE FIELD IF NOT EXISTS remotePreference ON TABLE search_profiles TYPE option<string>;
DEFINE FIELD IF NOT EXISTS excludedKeywords ON TABLE search_profiles TYPE array<string>;
DEFINE FIELD IF NOT EXISTS updatedAt ON TABLE search_profiles TYPE string;
DEFINE INDEX IF NOT EXISTS search_profiles_user_id ON TABLE search_profiles COLUMNS userId UNIQUE;

DEFINE TABLE IF NOT EXISTS job_postings SCHEMAFULL;
DEFINE FIELD IF NOT EXISTS canonicalUrl ON TABLE job_postings TYPE string;
DEFINE FIELD IF NOT EXISTS title ON TABLE job_postings TYPE string;
DEFINE FIELD IF NOT EXISTS company ON TABLE job_postings TYPE string;
DEFINE FIELD IF NOT EXISTS location ON TABLE job_postings TYPE string;
DEFINE FIELD IF NOT EXISTS remoteType ON TABLE job_postings TYPE option<string>;
DEFINE FIELD IF NOT EXISTS employmentType ON TABLE job_postings TYPE option<string>;
DEFINE FIELD IF NOT EXISTS salary ON TABLE job_postings TYPE option<string>;
DEFINE FIELD IF NOT EXISTS summary ON TABLE job_postings TYPE string;
DEFINE FIELD IF NOT EXISTS description ON TABLE job_postings TYPE option<string>;
DEFINE FIELD IF NOT EXISTS source ON TABLE job_postings TYPE string;
DEFINE FIELD IF NOT EXISTS publishedAt ON TABLE job_postings TYPE option<string>;
DEFINE FIELD IF NOT EXISTS tags ON TABLE job_postings TYPE array<string>;
DEFINE FIELD IF NOT EXISTS updatedAt ON TABLE job_postings TYPE string;
DEFINE INDEX IF NOT EXISTS job_postings_canonical_url ON TABLE job_postings COLUMNS canonicalUrl UNIQUE;

DEFINE TABLE IF NOT EXISTS job_matches SCHEMAFULL;
DEFINE FIELD IF NOT EXISTS userId ON TABLE job_matches TYPE string;
DEFINE FIELD IF NOT EXISTS jobId ON TABLE job_matches TYPE string;
DEFINE FIELD IF NOT EXISTS score ON TABLE job_matches TYPE number;
DEFINE FIELD IF NOT EXISTS reasons ON TABLE job_matches TYPE array<string>;
DEFINE FIELD IF NOT EXISTS matchedSkills ON TABLE job_matches TYPE array<string>;
DEFINE FIELD IF NOT EXISTS missingSkills ON TABLE job_matches TYPE array<string>;
DEFINE FIELD IF NOT EXISTS status ON TABLE job_matches TYPE string;
DEFINE FIELD IF NOT EXISTS createdAt ON TABLE job_matches TYPE string;
DEFINE FIELD IF NOT EXISTS updatedAt ON TABLE job_matches TYPE string;
DEFINE INDEX IF NOT EXISTS job_matches_user_job ON TABLE job_matches COLUMNS userId, jobId UNIQUE;

DEFINE TABLE IF NOT EXISTS notification_preferences SCHEMAFULL;
DEFINE FIELD IF NOT EXISTS userId ON TABLE notification_preferences TYPE string;
DEFINE FIELD IF NOT EXISTS subscribed ON TABLE notification_preferences TYPE bool;
DEFINE FIELD IF NOT EXISTS slotMinute ON TABLE notification_preferences TYPE int;
DEFINE FIELD IF NOT EXISTS updatedAt ON TABLE notification_preferences TYPE string;
DEFINE INDEX IF NOT EXISTS notification_preferences_user_id ON TABLE notification_preferences COLUMNS userId UNIQUE;

DEFINE TABLE IF NOT EXISTS broadcast_runs SCHEMAFULL;
DEFINE FIELD IF NOT EXISTS dateKey ON TABLE broadcast_runs TYPE string;
DEFINE FIELD IF NOT EXISTS windowStart ON TABLE broadcast_runs TYPE string;
DEFINE FIELD IF NOT EXISTS windowEnd ON TABLE broadcast_runs TYPE string;
DEFINE FIELD IF NOT EXISTS status ON TABLE broadcast_runs TYPE string;
DEFINE FIELD IF NOT EXISTS createdAt ON TABLE broadcast_runs TYPE string;
DEFINE FIELD IF NOT EXISTS updatedAt ON TABLE broadcast_runs TYPE string;
DEFINE INDEX IF NOT EXISTS broadcast_runs_date_key ON TABLE broadcast_runs COLUMNS dateKey UNIQUE;

DEFINE TABLE IF NOT EXISTS broadcast_deliveries SCHEMAFULL;
DEFINE FIELD IF NOT EXISTS runId ON TABLE broadcast_deliveries TYPE string;
DEFINE FIELD IF NOT EXISTS userId ON TABLE broadcast_deliveries TYPE string;
DEFINE FIELD IF NOT EXISTS chatId ON TABLE broadcast_deliveries TYPE string;
DEFINE FIELD IF NOT EXISTS scheduledFor ON TABLE broadcast_deliveries TYPE string;
DEFINE FIELD IF NOT EXISTS status ON TABLE broadcast_deliveries TYPE string;
DEFINE FIELD IF NOT EXISTS error ON TABLE broadcast_deliveries TYPE option<string>;
DEFINE FIELD IF NOT EXISTS sentAt ON TABLE broadcast_deliveries TYPE option<string>;
DEFINE FIELD IF NOT EXISTS updatedAt ON TABLE broadcast_deliveries TYPE string;
DEFINE INDEX IF NOT EXISTS broadcast_deliveries_run_user ON TABLE broadcast_deliveries COLUMNS runId, userId UNIQUE;
